{
  "name": "conversor-xml",
  "version": "1.0.0",
  "description": "Conversor de XML para DANFE e Excel",
  "scripts": {
    "build": "pip install -r requirements.txt && pip install requests==2.28.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 